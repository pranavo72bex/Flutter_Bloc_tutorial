import 'package:flutter/material.dart';

class WishListPagge extends StatelessWidget {
  const WishListPagge({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Wishlist Page'),
      ),
    );
  }
}
